;
; 1.10
;

(define (A x y)
    (cond ((= y 0) 0)
          ((= x 0) (* 2 y))
          ((= y 1) 2)
          (else (A (- x 1)
                   (A x (- y 1))))))

(A 1 10)
; 1024

(A 2 4)
; 65536

(A 3 3)
; 65536

(define (k n) (* 5 n n))
; => 5(n^2)

(define (f n) (A 0 n))
; (f n)
; (A 0 n)
; (* 2 n)
; => 2n

(define (g n) (A 1 n))
; n = 1 の時
; (A 1 1)
; => 2
;
; n = 2 の時
; (A 1 2)
; (A (- 1 1)
;    (A 1 (- 2 1)))
; (A 0
;    (A 1 1))
; (A 0 2)
; (* 2 (A 1 1))
; (* 2 2)
; => 4
; 
; n = 3 の時
; (A 1 3)
; (A 0
;    (A 1 2))
; (* 2 (A 1 2))
; (* 2 (* 2 2))
; => 8
;
; n = n の時
; 2 * 2^(n - 1)
; => 2^n

(define (h n) (A 2 n))
; n = 1 の時
; (A 2 1)
; => 2
;
; n = 2 の時
; (A 2 2)
; (A 1
;    (A 2 1))
; => (A 1 2)
; => 2 ^ 2
;
; n = 3 の時
; (A 2 3)
; (A 1
;    (A 2 2)
; => 2 ^ (2 ^ 2)
;
; n = 4 の時
; (A 2 4)
; (A 1
;    (A 2 3)
; => 2 ^ (2 ^ (2 ^ 2))
;
; 2 の n 回 累乗
